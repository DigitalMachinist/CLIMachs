
describe( 'CLIMachs CLI Namespaces', () => {

  describe( 'errors', () => {

    describe( 'CommandError', () => {

      it( 'contains spec with an expectation', () => {
        expect( true ).toBe( true );
      } );



    } );

  } );

  describe( 'cli', () => {

    describe( 'CommandPermissions', () => {

      it( 'contains spec with an expectation', () => {
        expect( true ).toBe( true );
      } );



    } );

    describe( 'CommandResponse', () => {

      it( 'contains spec with an expectation', () => {
        expect( true ).toBe( true );
      } );



    } );

    describe( 'Command', () => {

      it( 'contains spec with an expectation', () => {
        expect( true ).toBe( true );
      } );



    } );

    describe( 'CLI', () => {

      it( 'contains spec with an expectation', () => {
        expect( true ).toBe( true );
      } );



    } );

  } );

} );
